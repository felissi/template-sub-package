import { subPackageFunction } from "sub";

import { subModuleFunction } from "sub/sub-sub-module";

console.log(
  `🚀 // DEBUG 🍔  ~ file: main.ts:5 ~ `,
  subPackageFunction,
  subModuleFunction
);
